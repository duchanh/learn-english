import Feeds from '@/modules/FeedListing'
import { GetServerSideProps, GetServerSidePropsContext } from 'next'
// import { getFeedsSSRProps } from '@/fetch-data/home-props'

// export const getServerSideProps: GetServerSideProps = (context: GetServerSidePropsContext) => {
//   return getFeedsSSRProps(context)
// }

export default Feeds
